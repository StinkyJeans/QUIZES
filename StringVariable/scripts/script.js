const text = "This is a very long text";

const startPos = text.indexOf("long");

const len = text.length;
const lastVariable = text.substring(startPos, startPos + len);

console.log(lastVariable);


